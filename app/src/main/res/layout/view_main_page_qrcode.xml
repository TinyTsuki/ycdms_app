<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/pager_qrcode"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#EEEEEEEE"
    android:orientation="vertical">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical">

        <ImageView
            android:id="@+id/qrcode"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:layout_margin="20dp"
            android:adjustViewBounds="true"
            android:background="@drawable/bg_line"
            android:scaleType="fitXY"
            android:src="@drawable/icon_1024" />

        <com.lihang.ShadowLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginBottom="2dp"
            app:hl_layoutBackground="#0097FE"
            app:hl_layoutBackground_true="#1A689E"
            app:hl_shapeMode="ripple"
            app:hl_cornerRadius="12dp">

            <Button
                android:id="@+id/refresh_qrcode"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_margin="20dp"
                android:background="@drawable/bg_login_button"
                android:enabled="true"
                android:text="@string/action_refresh_qrcode"
                android:textColor="@color/white"
                android:textSize="18sp" />
        </com.lihang.ShadowLayout>
    </LinearLayout>
</FrameLayout>
